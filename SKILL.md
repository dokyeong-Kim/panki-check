---
name: panki-check
description: 기온과 네이버 검색량으로 의류 시즌 수요(판기)를 매일 판정하고 브리핑을 만드는 스킬. 후드집업·후드티·플리스의 검색시작·급상승·피크·종료 4단계를 4시즌(2022~2025) 실측으로 적합한 룰로 판정하고, 지난 단계는 확정하고 남은 단계는 예측한다. 사용자가 "판기 체크", "오늘 판기", "검색량 확인", "발매 타이밍", "시즌 수요", "기온 트리거", "후드티/후드집업/플리스 검색량"을 언급하거나, 발매일·기획전·광고 집행 시점을 기온과 엮어 판단하려 할 때 반드시 이 스킬을 쓸 것. "날씨 체크해줘"라고만 해도 시즌 판매 맥락이면 이 스킬을 쓴다. 매일 반복되는 루틴이므로 사용자가 짧게 말해도 트리거되어야 한다.
compatibility: Python 3.9+. 표준 라이브러리 + openpyxl(데이터랩 xlsx 임포트용). API 키는 references/setup.md 참고.
---

# 판기 체크

품목별 4단계(검색시작 → 급상승 → 피크 → 종료)를 매일 판정한다.
지난 단계는 검색지수로 **확정**하고, 남은 단계는 기온 룰로 **예측**한다.
목적은 날씨 보고가 아니라 **발주·광고·재고 결정 시점**이다.

## 매일 실행

```bash
python scripts/daily_check.py
```

임포트 → 검색지수 갱신 → 기온 갱신 → 판정 순으로 돈다. 네이버 API가 막힌
Cowork 환경에서는 `PANKI_SEARCH_CSV_URL`이 설정돼 있으면 GitHub/외부의 최신
`search_daily.csv`를 자동으로 받아온다. 둘 다 실패하면 마지막 정상 데이터로 판정한다.

**사용자가 "판기 체크"라고 하면:** 이 스킬 폴더를 `/home/claude/panki-check`로
복사한 뒤 위 명령을 실행하고, 아래 "보고하는 법"대로 다시 쓴다.
스킬 폴더는 읽기 전용이라 `data/`에 쓰려면 복사본에서 돌려야 한다.

## 데이터 갱신

### 자동 (권장)

GitHub Actions가 매일 네이버 DataLab API를 호출해 `data/search_daily.csv`를
갱신한다. Cowork는 네이버 직접 호출이 `hostname_blocked`되면
`PANKI_SEARCH_CSV_URL`의 최신 CSV를 대신 받아온다. 자세한 설정은
`references/github_sync.md` 참고.

### 수동 fallback

자동 동기화를 쓰지 않을 때는 사용자가 두 가지를 올리면 `import_manual.py`가 자동으로 잡는다.

1. **데이터랩 xlsx** — datalab.naver.com 검색어 트렌드, 그룹 3개를 **한 번에**:
   `후드집업`(후드집업, 기모후드집업) / `후드티`(후드티, 기모후드티) /
   `플리스`(플리스, 후리스). 기간 2022-01-01 ~ 어제, 일간, 전체 기기·성별·연령.
   기간이 바뀌면 정규화가 바뀌므로 항상 통째로 덮어쓴다.
2. **ASOS CSV** — 기상자료개방포털 서울(108) 일자료. 연도별 파일 여러 개를
   한 번에 올려도 된다. 기존 것과 병합한다.

검색지수가 7일 넘게 묵으면 브리핑 상단에 경고가 뜬다. 그때 xlsx를 다시 받으라고 한다.

**예보는 웨더뉴스에서 자동으로 긁는다** (`fetch_weathernews.py`, 키 불필요,
16일 일별 최고/최저). URL은 `config/triggers.json`의 `forecast.weathernews_url`.
기상청 API가 열려 있으면 기상청 값이 우선하고, 웨더뉴스는 그 뒤를 채운다.
평균기온은 (최고+최저)/2 근사 — 플리스 급상승 룰에만 쓰인다.

## 판정 룰 (config/triggers.json)

| 품목 | 검색시작 | 급상승 | 피크 | 종료 |
|---|---|---|---|---|
| 후드집업 | 달력 8/12 (8/9~8/15) | 평균 28℃ 5일 → +9일 (±4.0) | 최저 12.5℃ 2일 → +5일 (±0.7) | 평균 7℃ → +22일 (±6.4) |
| 후드티 | 달력 8/11 (8/8~8/14) | 평균 28℃ 5일 → +10일 (±2.9) | 최저 15℃ 5일 → +8일 (±1.1) | 최고 21℃ 이상 4일 → +4일 (봄, ±3.3) |
| 플리스 | 달력 8/29 (8/21~9/4) | 최저 14℃ → +2일 (±1.3) | 최저 11℃ 5일 → +6일 (±1.2) | 최고 2.5℃ 2일 → +9일 (±4.2) |

**플리스 급상승은 집업·후드티보다 한 달 늦다** (9월 말~10월 중순). 브리핑에서
플리스가 먼저 나오면 정의가 깨진 것이다.

**검색시작만 기온으로 못 잡는다.** 최적 기온룰이 8/1 탐색 경계에 붙어(최저
28℃가 4년 중 3년 8/1) 실질은 "8/1+N일"이다. 달력 편차가 2.3~5.2일로 낫다.
급상승·피크·종료는 전부 기온 룰이다.

**피크는 기온으로 정확히 잡힌다.** 세 품목 모두 편차 1일 이내.
10월 초 실측 기온이 들어오는 순간 피크일이 거의 확정된다.

검색 단계 확정 기준(judge.py의 DET): 베이스 = 7/1~8/10 7일평균 중앙값.
**베이스 = 7/15~8/10 7일평균의 최솟값(여름 저점).** 구 정의(7/1~8/10 중앙값)는
6월 수요의 7월 감쇠 꼬리가 섞여 후드집업 베이스를 29% 부풀렸다.
시작 = 저점 × **배수(집업·후드티 1.5 / 플리스 2.0)** 3일 연속,
급상승 = 저점 + 0.15×(전년 피크 − 저점) 3일 연속,
피크 = 시즌 최대(최대 후 7일 경과 + 10% 하락 시 확정), 종료 = 피크 이후
베이스+20% 이하 3일 연속.

급상승을 베이스 배수로 잡으면 안 되는 이유: 플리스는 피크/베이스가 47배,
집업·후드티는 15~20배다. 전년 피크 대비 비율이 세 품목 공통 기준이다.
반대로 시작은 전년 피크 대비로 잡으면 플리스가 9월 말로 밀려서 저점 배수를 쓴다.
배수가 품목마다 다른 건 플리스의 여름 절대량이 작아 노이즈가 크기 때문(AZA 확정).

**예상일이 지났는데 미도달이면** 기온 룰을 버리고 검색 상승 속도로 재추정하고
⚠지연을 붙인다. 2026 집업·후드티 급상승이 그 경우다 — 기온은 8/12에 조건을
넘었는데 검색이 안 따라왔다.

## 예측은 항상 일자 범위로

"9월 중하순" 같은 표현은 쓰지 않는다. 모든 예측은 `MM/D~MM/D`로 나온다.

- 기온 룰 단계(피크·종료·플리스 급상승): 트리거일 + 리드타임 ± 적합 편차.
  예보 안에 트리거가 보이면 🔷, 실측으로 발화했으면 🔶, 둘 다 없으면 4년 평균 ⏳
- 달력 단계(시작·급상승): 현재 7일평균 상승 속도로 배수 도달일을 외삽.
  최근 5일 기울기(빠른 쪽)와 10일 기울기(느린 쪽, +30% 여유)로 범위를 만든다.
  이미 기준에 닿아 유지 중이면 "N일째 → 확정 예정일"로 표시.
  옆에 "기온 MM/D부터 예년 발생 수준 이하"가 붙는다 — 예측 룰이 아니라
  "기온은 조건 충족, 남은 건 MD 캘린더"라는 뜻이다

## 브리핑에 뜨는 표시

- ✅ 확정 — 검색지수로 실제 관측된 것. **표에서 이것만 이모지를 쓴다**
- ⚠ 지연 — 예년 창을 지났는데 미검출
- 나머지 셀은 날짜 범위만 쓴다. 확신도 이모지(🔶🔷⏳)는 표에 넣지 않는다

확신도는 표가 아니라 **판정 근거 줄글**에서 말한다 — "실측 기온으로 발화했다",
"예보 기준이다", "4년 평균이라 예보가 들어오면 바뀐다" 같은 식으로.

## 사용자에게 보고하는 법

`judge.py`가 끝에 `data/brief_latest.md`를 만든다. **그 내용을 그대로 보여준 뒤
"판정 근거"를 줄글로 다시 쓴다.** 형식은 고정이다.

```
**오늘 브리핑 (M/D, 예보원 반영, 검색데이터 M/D, 날씨데이터 M/D)**

| 품목 | 검색시작 | 급상승 | 피크 | 종료 |
|---|---|---|---|---|
| 후드집업 | ... | ... | ... | ... |
| 후드티 | ... | ... | ... | ... |
| 플리스 | ... | ... | ... | ... |

**판정 근거**
(줄글. 아래 규칙대로)
```

판정 근거 줄글 규칙:

1. **예보부터** — 언제부터 기온이 꺾이는지 한 문장. 예: "9/5부터 최저 18℃대로 꺾인다."
2. **바뀐 것** — 어제와 달라진 셀(✅ 확정, 🔶→🔷 승격, ⚠ 새 경고)이 있으면 그것부터. 없으면 "변화 없음".
3. **셀마다 왜 그 날짜인가** — 표에 나온 날짜 하나하나에 대해 "어떤 기준(급상승선 %, 기온 트리거)이 어떤 데이터(예보/실측/4년 평균)에서 어떻게 나왔는지" 한두 문장. `brief_latest.md`의 근거 목록을 그대로 나열하지 말고, 읽는 사람이 흐름으로 이해하게 이어 쓴다.
4. **어긋나는 신호가 있으면 명시** — 예: "기온은 조건을 넘었는데 검색이 굼뜨다. 예년엔 없던 구도라 다음 주 데이터가 관건." 순서 역전 경고가 있으면 반드시 포함.
5. ⏳ 셀은 근거를 길게 쓰지 않는다 — "4년 평균 기준, 예보가 들어오면 갱신" 한 줄.

쓰지 말 것: "9월 중하순" 같은 뭉뚱그린 표현(항상 M/D~M/D), 매일 같은 분량의 반복 설명,
`judge.py` 원문 붙여넣기. 변화 없는 날은 표 + 근거 세 줄이면 된다.

## 룰을 고칠 때

숫자는 `config/triggers.json`에만 있다. 바꾸면 `references/validation.md`에
날짜·근거를 남긴다. 임계값을 재적합하려면 `references/validation.md`
2026-09-02(4) 항의 방법(전수 탐색 + 리드타임 0~30일 제약)을 그대로 쓴다.
**리드타임 편차만 최소화하면 7월에 발화해 10월을 맞히는 우연 룰이 뽑힌다.**

## 함정

- **원본 일간지수로 판단하지 않는다.** 일요일마다 튄다. 항상 7일 이동평균.
- **데이터랩은 상대지수다.** 기간을 바꾸면 전체가 재정규화된다. 증분 저장 금지.
- **세 그룹은 한 요청에 묶는다.** 따로 받으면 품목 간 비교 불가.
- **여름 일시 저온에 속지 않는다.** 기온 트리거 탐색은 8/1부터. 7월에 최저 20℃
  찍히는 날이 있어도 급상승 트리거가 아니다.
- **예보와 실측을 구분해 말한다.** 중기예보 구간 D-7 이상은 확정처럼 말하지 않는다.

## 파일 구조

```
config/triggers.json      룰 + 4년 평균 도달일. 여기 숫자만 고친다
data/search_daily.csv     검색지수 일간 (import 산출)
data/weather_daily.csv    ASOS 실측 (import 산출)
data/weather.json         기상청 API 산출 (실측+예보). 없어도 됨
data/brief_latest.txt     최근 브리핑
data/history.csv          일일 판정 누적
scripts/daily_check.py    진입점
scripts/import_manual.py  xlsx/CSV → 표준 CSV
scripts/fetch_weathernews.py 웨더뉴스 16일 예보 → weather.json (키 불필요)
.github/workflows/update-naver-search.yml GitHub에서 네이버 검색지수 매일 자동수집
scripts/judge.py          판정 + 브리핑
scripts/judge_v1_legacy.py 구 버전(2026-08 단일 임계 방식). 참고용
references/validation.md  룰 변경 이력과 근거
references/setup.md       API 키 발급
```
